{
	"features": ["simplePagesApiRoutes"],
	"localSetup": "npm install",
	"buildConfig": {
		"buildCommand": "npx @cloudflare/next-on-pages --experimental-minify",
		"buildOutputDirectory": ".vercel/output/static"
	},
	"deploymentConfig": {
		"compatibilityFlags": ["nodejs_compat"]
	}
}
